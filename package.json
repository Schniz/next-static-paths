{
  "name": "next-static-paths-monorepo",
  "private": true,
  "scripts": {
    "build": "FORCE_COLOR=1 turbo run build",
    "lint": "FORCE_COLOR=1 turbo run lint",
    "test": "FORCE_COLOR=1 turbo run test",
    "changeset:version": "changesets version && pnpm install"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "turbo": "^1.2.9"
  }
}
